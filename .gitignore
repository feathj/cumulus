node_modules/
dist/
.next/
out/

# Local environment — .env.example is the checked-in template
.env
.env.local
.env.*.local

# Database backups produced by the JSON export
backups/*.json

# Personal data dumps and the import config that maps them — never committed
data/

.DS_Store
*.log
